<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sahasu.lazypizza">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:label="Lazy Pizza">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" android:theme="@style/AppTheme.TransNav" />
        <activity android:name=".MarketPlaceOrder" />
        <activity android:name=".Menu_PlaceOrder" />
        <activity android:name=".GoogleLogin" />
        <activity android:name=".PhoneNumber" />
        <activity android:name=".MyOrders" />
        <activity android:name=".orderDetails" />
        <activity android:name=".MyBalance" />
        <activity android:name=".SplashScreen" />

        <activity android:name=".ProfilePage"></activity>
    </application>

</manifest>